import Header from './Header';
import CryptoContainer from './CryptoContainer';

// The reaso we can access state.crypto is because we grab cryptocontainer from here
export { Header, CryptoContainer };