import { AppRegistry } from 'react-native';
import App from './src/App';

// index.js is mandatory but you can change App.js, App.js is only a defualt root view

AppRegistry.registerComponent('auth', () => App);
